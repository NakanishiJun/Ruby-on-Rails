<%= form_with(model: distributor, local: true) do |form| %>
  <% if distributor.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(distributor.errors.count, "error") %> prohibited this distributor from being saved:</h2>

      <ul>
      <% distributor.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :distributor_name %>
    <%= form.text_area :distributor_name %>
  </div>

  <div class="field">
    <%= form.label :distributor_rep %>
    <%= form.text_area :distributor_rep %>
  </div>

  <div class="field">
    <%= form.label :distributor_rep_tel %>
    <%= form.number_field :distributor_rep_tel %>
  </div>

  <div class="field">
    <%= form.label :distributor_rep_mail %>
    <%= form.text_area :distributor_rep_mail %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
